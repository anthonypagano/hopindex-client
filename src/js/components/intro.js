import React from 'react';

export default function Intro() {
    return (
        <section aria-live="polite">
            The Hop Index lets you log the craft beers you have tried, who and where they were made, their ABV, your rating, and a place for notes.
        </section>
    );
}